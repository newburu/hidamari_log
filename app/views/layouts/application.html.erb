<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Hidamari Log" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <%# --- Google Analytics --- %>
    <% if Rails.env.production? && ENV['GOOGLE_ANALYTICS_ID'].present? %>
      <script async src="https://www.googletagmanager.com/gtag/js?id=<%= ENV['GOOGLE_ANALYTICS_ID'] %>"></script>
      <script>
        window.dataLayer = window.dataLayer || [];
        function gtag(){dataLayer.push(arguments);}
        gtag('js', new Date());

        gtag('config', '<%= ENV['GOOGLE_ANALYTICS_ID'] %>');
      </script>
    <% end %>
    <%# --- End Google Analytics --- %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container-fluid">
        <a class="navbar-brand" href="/">Hidamari Log</a>
        <div class="ms-auto d-flex align-items-center gap-2">
          <% if current_user %>
            <span><%= current_user.name %>さん</span>
            <%= button_to "ログアウト", logout_path, method: :delete, class: 'btn btn-outline-secondary btn-sm' %>
          <% else %>
            <%= link_to "Googleでログイン", "/auth/google_oauth2", class: 'btn btn-outline-primary' %>
            <%= link_to "Xでログイン", "/auth/twitter2", class: 'btn btn-outline-info' %>
          <% end %>
        </div>
      </div>
    </nav>

    <div class="container mt-4">
      <% if flash[:notice] %>
        <div class="alert alert-success" role="alert">
          <%= flash[:notice] %>
        </div>
      <% end %>
      <%= yield %>
    </div>

    <footer class="footer mt-auto py-3 bg-light border-top">
      <div class="container text-center">
        <span class="text-muted">
          <%= link_to '利用規約', terms_path, class: "text-muted mx-2" %> |
          <%= link_to 'プライバシーポリシー', privacy_path, class: "text-muted mx-2" %>
        </span>
      </div>
    </footer>

  </body>
</html>
